<p id="notice"><%= notice %></p>

<h1>Questions</h1>

<table class="table">
  <thead>
    <tr>
      <th>Conteúdo</th>
      <th>Fonte</th>
      <th>Ano</th>
      <th>Status</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @questions.each do |question| %>
      <tr>
        <td><%= question.content %></td>
        <td><%= question.source %></td>
        <td><%= question.year %></td>
        <td><%= question.status %></td>
        <td><%= link_to 'Show', question, class: 'btn btn-primary' %></td>
        <td><%= link_to 'Edit', edit_question_path(question), class: 'btn btn-primary' %></td>
        <td><%= link_to 'Destroy', question, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-primary' %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Question', new_question_path %>
